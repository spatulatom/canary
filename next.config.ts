import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable stable TypeScript features from 15.5
  // typedRoutes: true,

  experimental: {
    // Enable any experimental features we want to showcase
  },
};

export default nextConfig;
